{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Launch Program",
      "skipFiles": ["<node_internals>/**"],
      "program": "${workspaceFolder}/app/layout.js", // Entry point for your app
      // "preLaunchTask": "tsc: build - tsconfig.json"
    }
  ]
}
